#!/bin/bash

export DIR=/Users/swade/projects/actuate/website

#echo "starting: " `pwd`
#echo "Generating site with Hugo for website"

#cd $DIR
#echo "hugo: " `pwd`
#hugo

#cd $DIR/../testuation.github.io #&& git add .
echo "git: " `pwd`
#echo `ls .`
git add -A
git commit -a -m "update (auto)"
#git push origin master

echo "All done! 0 problems"
